echo 'hello'
cd bin
# # start server
# air &
# P1=$!
# # reloads browser whenever a new go binary is built
# sleep 2 && browser-sync start --proxy "localhost:8080" --files "bin/main" &
# P2=$!
# wait $P1 $P2